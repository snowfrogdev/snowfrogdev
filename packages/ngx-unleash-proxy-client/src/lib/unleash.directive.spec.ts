import { UnleashDirective } from './unleash.directive';

describe('UnleashDirective', () => {
  it('should create an instance', () => {
    const directive = new UnleashDirective();
    expect(directive).toBeTruthy();
  });
});
